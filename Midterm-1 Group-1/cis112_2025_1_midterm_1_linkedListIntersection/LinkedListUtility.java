package cis112_2025_1_midterm_1_linkedListIntersection;

/**
 * Union of two linked lists
 */
public class LinkedListUtility {

	private static final boolean DEBUG = true;
	private static final boolean DEBUG_M = true;
	private static final boolean DEBUG_M2 = true;

	
	/**
	 * Given two linked lists list1 and list2,
	 * find list1 intersection list2. That is to say, find all
	 * common elements in list1 which are also in list2.
	 * 
	 * Example: list1 = a, b, c.  and list2 = c, d, e. 
	 * Then, the list c. is returned.
	 * 
	 * @param list1, a linked list of Nodes.
	 * @param list2, a linked List of Nodes.
	 * @return list1 ∩ list2. The order of nodes in the intersection
	 * is the same as list1.
	 */
	
	public static LinkedList intersection(LinkedList list1, LinkedList list2) {
		if (DEBUG_M2) {
			System.out
					.println("\n->" + StackWalker.getInstance().walk(s -> s.skip(0).findFirst()).get().getMethodName());
		}
		
		LinkedList resultList = new LinkedList();

		// below-0123456789-V toDo // do not change this line ~~~~~~~~~~ V
		// TODO your code should be between below and above marks.

		// below-0123456789-9876543210-V solution >>>>>>>>> V
		
		
	

		// above-0123456789-9876543210-A solution <<<<<<<<< A

		// above-0123456789-A toDo // do not change this line ~~~~~~~~~~ A
	
		return resultList;
	}
}

